# Dependencies
node_modules/

# Build output
out/
dist/
release/
*.tsbuildinfo

# Electron build caches
.electron-builder-cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.*
!.env.example

# Local Octo workspace data (per-workspace specs/hooks/steering belong with the user's
# project, not this repo). `.kraken/` is the pre-rename name, kept so a workspace opened
# before f54c4cb doesn't show up as untracked.
.octo/
.kraken/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
